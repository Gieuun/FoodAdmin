<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">
<mapper namespace="mybatis.FoodDBMapper">

	<insert id="insertFoodDB"
		parameterType="com.sds.foodadmin.domain.FoodDB">
		INSERT INTO foodDB (foodname, kcal, carbohydrate, protein, fat, sugar,
		sodium)
		VALUES (#{foodname}, #{kcal}, #{carbohydrate}, #{protein}, #{fat}, #{sugar},
		#{sodium})
	</insert>

	<select id="selectAll">
		SELECT * FROM foodDB;
	</select>

	<update id="updateFoodDB"
		parameterType="com.sds.foodadmin.domain.FoodDB">
		UPDATE foodDB SET kcal=#{kcal}, carbohydrate = #{carbohydrate}, protein =
		#{protein}, fat = #{fat}, sugar = #{sugar}, sodium = #{sodium}
		WHERE foodIdx = #{foodIdx} AND foodname = #{foodname};
	</update>

	<delete id="deleteFoodDB"
		parameterType="com.sds.foodadmin.domain.FoodDB">
		DELETE FROM foodDB WHERE foodIdx=#{foodIdx} AND foodname = #{foodname};
	</delete>

</mapper>